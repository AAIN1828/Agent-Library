{
  "agent_id": "response_delivery",
  "name": "Response Delivery",
  "role": "Confidence-aware response delivery and post-generation evaluation",
  "description": "Polishes the grounded draft into the final user-facing answer, preserves conflict/confidence wording, and scores the output with the shared post-generation evaluation rubric before returning to the user.",
  "flow_name": "Delivery",
  "version": "1.0.0",
  "entry_point": {
    "module": "agent",
    "factory": "build_agents",
    "entry_agent": "Response_Delivery_Agent",
    "runner": "ResponseDeliveryRunner.run"
  },
  "sub_agents": [
    {
      "name": "Response_Delivery_Agent",
      "type": "AssistantAgent",
      "purpose": "Polish final answer and emit post-generation evaluation scores.",
      "max_consecutive_auto_reply": 4
    }
  ],
  "handoff_targets": ["user_proxy"],
  "dependencies": {
    "python_packages": ["pyautogen", "openai"],
    "external_resources": ["Azure OpenAI"]
  },
  "requires_llm_config": true,
  "configuration_parameters": [
    { "name": "GPT4_LLM_MODEL_DEPLOYMENT_NAME", "secret": false, "required": true },
    { "name": "AZURE_OPENAI_API_BASE", "secret": false, "required": true },
    { "name": "AZURE_OPENAI_API_KEY", "secret": true, "required": true },
    { "name": "AZURE_OPENAI_API_VERSION", "secret": false, "required": true }
  ],
  "statefulness": "build_agents() is a factory — call once per conversation/session."
}
